# 赛事预言家项目总需求 v1.0

> 状态：规划基线。业务规则唯一来源仍是 `MVP__v1.0.md`；本文不修改其字段、状态、错误码或领域语义。
>
> 目标：按“核心后端冻结 → 非核心后端 → 前端 UI”的顺序完成赛事预言家 MVP，避免前端因后端契约变化反复返工。

## 1. 产品范围

- 产品：英超 `2026_2027` 赛事比分预测 MVP。
- 技术基线：TypeScript、微信云开发目标环境、API-Football 数据源。
- 当前基线：后端领域、预测、注入式 Provider 同步、结算账本、统计重建与管理端最小闭环已提交；前端 UI、真实数据库/Provider/部署接线尚未完成。
- 核心原则：可信 `openid` 由网关注入；不发明 JWT；未定义行为 Fail Closed；账本为事实来源；不物理删除核心业务事实。

## 2. 开发顺序与阶段门禁

| 阶段 | 目的 | 进入条件 | 离开条件 |
|---|---|---|---|
| A：核心后端冻结 | 固定业务规则、核心 API 合同和关键状态流 | 当前后端提交基线 | 高/中优先级规则空洞关闭；核心 API/OpenAPI/测试一致 |
| B：非核心后端 | 连接真实基础设施并补齐运行能力 | 阶段 A 通过 API 冻结检查 | 可在目标环境持久化、同步、调度、观测和恢复 |
| C：前端 UI | 设计并实现用户与管理界面 | 阶段 A 的用户端 API 契约稳定 | 核心用户路径可用；前端状态与后端语义一致 |

阶段之间不得通过“前端先猜接口”或“真实 Provider 先倒逼领域规则”绕过门禁。

## 3. 阶段 A：核心后端冻结需求

### A1. 管理端写操作契约

冻结赛果修正、结算重试、用户统计重建、排行榜重建的：

- 请求字段与 `reason` 的来源；无人工输入时的系统审计原因。
- 授权、前置条件、锁行为、幂等语义和审计结果。
- `waiting`、`settling`、`correcting`、`failed`、`settled` 下 retry 的允许性、目标 settlement 选择、HTTP 状态和业务错误码。
- 部分成功时返回的 `outcome`、计数和审计摘要口径。

### A2. API 响应契约

所有将被前端调用的接口必须冻结：字段名、类型、nullable、数组/空值语义、排序、分页 cursor、时间格式、成功 envelope、失败 HTTP/code 和 OpenAPI schema。

优先覆盖：

- `GET /v1/predictions/me`
- `GET /v1/unlocks/me`
- `GET /v1/admin/anomalies`
- 管理端写接口的成功和失败响应

### A3. 时间、任务、重试和锁

- 业务判断只使用可注入的可信 `server_now`。
- Provider retry 的等待和随机 jitter 必须可注入或可确定性测试。
- 锁 lease 的获取、续租时机、续租失败后的停止行为必须明确。
- 业务层只定义“现在是否应执行”；调度层单独定义频率与触发器。

### A4. 剩余结算状态写入审查

以 `settlement_status` 直接写入点为范围，区分：

- 新建比赛的初始 `pending`：允许。
- Provider 生命周期更新：必须符合冻结状态机。
- 管理员赛果修正和结算：必须通过统一状态转移入口或有明确、受测的例外。

只修违反状态机的路径，不做无目标的大重构。

## 4. 阶段 B：非核心后端需求

阶段 B 不得改变阶段 A 已冻结的业务规则和 API 语义。

1. 真实 CloudBase/数据库 repository 与迁移、事务、索引接线。
2. 真实 API-Football client：认证、配额、超时、错误分类和 payload 保留。
3. 云函数/HTTP 路由、环境配置和部署接线。
4. future/full/near/live/post-finish/daily consistency 的生产调度。
5. daily consistency anomaly 的专用数据模型与幂等落库（须先冻结 schema/type）。
6. 多实例可用的共享限流存储。
7. 结构化日志、告警和运营诊断：同步失败、账本失败、Provider 冲突、管理员操作。

## 5. 阶段 C：前端 UI 需求

前端在阶段 A 的接口冻结检查通过后开始，先做用户路径与信息架构，再做视觉设计和页面实现。

### 用户端核心路径

1. 首次进入与 session 初始化。
2. 比赛列表与筛选。
3. 比赛详情与预测提交。
4. 我的预测、结算结果、等级、解锁和排行榜。
5. 预测不可提交、截止、延期、未登录、注销、加载、空数据和错误反馈。

### 管理端

管理端异常查看、赛果修正、结算 retry、重建操作是否进入首版 UI，单独在 UI 设计前确认；不得默认扩大范围。

## 6. 前端开始前的 API 冻结检查

以下全部通过后，方可进入页面和视觉设计：

- 用户端页面所需接口均有 OpenAPI schema。
- 成功响应、错误码和 HTTP 状态固定。
- cursor、排序、时间字段、nullable 和空数组语义固定。
- 用户态、比赛态、预测态和结算态可以映射为明确的页面状态。
- 前端所需的 Provider/anomaly/admin 展示字段已明确。

## 7. 质量与提交要求

每个开发切片应遵循：先失败测试、最小实现、独立复核。完成前至少执行：

```sh
npm run typecheck
npm test -- --run
npm run build
git diff --check
```

不提交凭证、`.env`、真实数据库、运行日志或 Agent 监督器输出。未完成功能必须在文档中明确标识，不得宣称 MVP 已完整上线。
